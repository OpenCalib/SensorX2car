{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",  // 编译任务的名称，跟上面preLaunchTask的值对应
            "type": "shell",
            "command": "bash ./.vscode/build.sh",  // 编译器的路径，须跟你自己的电脑相符
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}